import type { Metadata } from "next";import Link from "next/link";import { AuthShell } from "@/components/auth-shell";import { ResetPasswordForm } from "@/components/auth-forms";
export const metadata:Metadata={title:"Atur Ulang Kata Sandi",robots:{index:false,follow:false}};export default async function Page({searchParams}:{searchParams:Promise<{token?:string}>}){const token=(await searchParams).token??"";return <AuthShell title="Atur kata sandi baru" description="Gunakan kata sandi unik yang belum dipakai pada layanan lain. Tautan pemulihan akan divalidasi saat Anda menyimpan perubahan." footer={<Link className="font-medium text-primary underline" href="/forgot-password">Minta tautan baru</Link>}><ResetPasswordForm token={token}/></AuthShell>}
