import type { Metadata } from "next";import Link from "next/link";import { redirect } from "next/navigation";import { AuthShell } from "@/components/auth-shell";import { LoginForm } from "@/components/auth-forms";import { getSessionUser } from "@/lib/auth";
export const metadata:Metadata={title:"Masuk",robots:{index:false,follow:false}};export default async function Page({searchParams}:{searchParams:Promise<{next?:string;verified?:string;error?:string}>}){const user=await getSessionUser();if(user)redirect(user.role==="customer"?"/dashboard":"/admin");const params=await searchParams;return <AuthShell title="Masuk ke WangStore" description={params.verified?"Email berhasil diverifikasi. Silakan masuk.":params.error?"Verifikasi email belum berhasil. Coba gunakan kembali tautan terbaru.":"Akses pesanan, layanan, tiket, dan konfigurasi tersimpan."} footer={<>Belum punya akun? <Link className="font-medium text-primary underline" href="/register">Daftar</Link></>}><LoginForm nextPath={params.next}/></AuthShell>}
