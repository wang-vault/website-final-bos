import type { Metadata } from "next";import Link from "next/link";import { AuthShell } from "@/components/auth-shell";import { ForgotPasswordForm } from "@/components/auth-forms";
export const metadata:Metadata={title:"Lupa Kata Sandi",robots:{index:false,follow:false}};export default function Page(){return <AuthShell title="Pulihkan akses akun" description="Untuk menjaga privasi, respons yang sama ditampilkan baik email terdaftar maupun tidak." footer={<Link className="font-medium text-primary underline" href="/login">Kembali ke login</Link>}><ForgotPasswordForm/></AuthShell>}
