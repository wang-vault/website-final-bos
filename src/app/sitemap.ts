import type { MetadataRoute } from "next";
import { listContent,listPublicPages } from "@/lib/db/repository";
export const dynamic="force-dynamic";
export default async function sitemap():Promise<MetadataRoute.Sitemap>{const base=process.env.NEXT_PUBLIC_APP_URL??"http://localhost:3000";const paths=["","/about","/infrastructure","/server-builder","/features","/why-wangstore","/faq","/testimonials","/blog","/knowledge-base","/status","/contact","/vps","/terms","/privacy","/refund","/sla","/acceptable-use","/cookie-policy"];let blog:Awaited<ReturnType<typeof listContent>>=[];let knowledge:Awaited<ReturnType<typeof listContent>>=[];let pages:Awaited<ReturnType<typeof listPublicPages>>=[];let legal:Awaited<ReturnType<typeof listPublicPages>>=[];try{[blog,knowledge,pages,legal]=await Promise.all([listContent("blog"),listContent("knowledge"),listPublicPages("pages"),listPublicPages("legal")])}catch{}return[...paths.map(path=>({url:`${base}${path}`,lastModified:new Date(),changeFrequency:path===""?"weekly" as const:"monthly" as const,priority:path===""?1:.7})),...blog.map(item=>({url:`${base}/blog/${item.slug}`,lastModified:new Date(item.updatedAt),changeFrequency:"monthly" as const,priority:.6})),...knowledge.map(item=>({url:`${base}/knowledge-base/${item.slug}`,lastModified:new Date(item.updatedAt),changeFrequency:"monthly" as const,priority:.6})),...pages.map(item=>({url:`${base}/p/${item.slug}`,lastModified:new Date(item.updatedAt),changeFrequency:"monthly" as const,priority:.5})),...legal.map(item=>({url:`${base}/legal/${item.slug}`,lastModified:new Date(item.updatedAt),changeFrequency:"yearly" as const,priority:.4}))]}
